spring:
  application:
    name: flyway
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ddd?createDatabaseIfNotExist=true
    username: root
    password: root
  flyway:
    user: root
    password: root
    clean-disabled: true
    locations: classpath:/db/migration
    url: jdbc:mysql://localhost:3306/ddd?createDatabaseIfNotExist=true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: true

#logging:
#  level:
#    root: debug
